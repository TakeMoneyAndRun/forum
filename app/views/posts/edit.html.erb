
<h1>Editing post</h1>
<%= link_to @forum.name, forum_topics_path(@forum) %>

<%= form_for(@post, :url => forum_topic_post_path(@topic.forum,@topic, @post)) do |f| %>
    <% if @topic.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

          <ul>
            <% @topic.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :body %><br />
      <%= f.text_area(:body, :size => "60x10")%>
    </div>


    <div class="actions">
      <%= f.submit%>
    </div>
<% end %>

<%= link_to 'Back', [@topic.forum,@topic]  %>